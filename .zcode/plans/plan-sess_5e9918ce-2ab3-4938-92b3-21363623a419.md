通道拟合函数（中枢主导三步法）+ 绿色外层通道：

1. 新文件 core/channel.py：fit_channel(df, start, end, coverage=0.9, tilt=0.12, pivot_k=None)——①中枢LSQ（K线中点）锁定趋势位置与角度；②小角度倾斜搜索（±tilt，默认±12%），双轨始终平行；③每档斜率下两轨张合压住摆动极值点（±k根局部极值，k默认≈每日根数；分位数容忍近似点）；④选宽度最小的斜率。返回端点(pos+价)/斜率/下探上张量/覆盖率/被压点清单。纯计算不碰plotly。
2. render/primitives.py 新增 channel 原语：{type: channel, from/to=中枢端点, lower/upper=下探/上张量, color, dash, label?} 画上下两条平行轨（可不对称），可选label标在通道上。
3. plugins/daily_candle.py：ANN_TYPES 加 channel；新参数 channels=[{start,end,coverage,tilt,color,dash,label}]，插件调 fit_channel 自动产出通道图层（换数据自动重算）。
4. configs/daily_candle.yaml：黄色通道改一条 channels 自动拟合声明（窗口07-27→08-18）；新增绿色外层通道（07-22 09:45→08-26 15:00、coverage 0.9、绿虚线 #39d353）。渲染后如黄色形态与已认可版本有出入则微调 tilt/coverage。
5. 测试：test_channel.py 合成通道参数恢复/两低点被压住；channel 原语两轨偏移（含不对称）；插件 channels 端到端；pytest -q 全绿（预计110+）。
6. 渲染目检（绿色在外、黄色在内、极值被压住）→ 提交。