P0+P1 防坑能力实施 + 待办文件夹：

1. core/plugins.py：StrategyOutput 增加 notes 字段（插件→脚注回显通道）。
2. plugins/daily_candle.py：新增 _resolve_anchor——通道端点支持事件式锚定（{peak/trough: true}、{above/below: 价, after?: 日期}），数据刷新自动重锚；解析结果写入 out.notes 回显（如"通道1锚点解析: 2025-02-07→2026-08-10"）；无命中/非法规则中文报错。
3. core/pipeline.py：out.notes 并入脚注 notes；forecast_days 设置时回显"预测区: N工作日 ≈ M根"（单位换算防混淆）。
4. render/figure_daily.py：内置渲染可见性守卫——出图后扫描全部数据坐标元素（traces/annotations含箭尾/shapes），超出 xaxis 范围即在脚注追加"渲染可见性警告"（不再依赖每份清单复制守卫）。
5. qa/verify.py：新增 expect_upper_wraps(points)（上轨压住高点带，与 expect_lower_wraps 对称）、expect_in_left_quarter(x)、expect_in_forecast_zone(x)（按 n_all 动态计算，替代清单手写硬编码区间）。
6. tests：规则锚定四态（peak/above/below/无命中/非法）、管线回显断言、可见性守卫负向验证（超界元素→脚注出现警告）、三个新 Verifier 方法正反例。
7. docs/backlog/README.md（新建待办文件夹）：分段器 P2 暂缓入册（记录你的理由：通道拟合主观性强、无前置量化标准时视觉判断不精确、需人工明确时间段）；同步迁移历次缓办事项（R检查带状灵敏度、参数式断言不读fig、CSV改名、MA partial 语义、对照图入库策略等）。
8. 设计文档第一篇同步（FR-4 可见性守卫与回显、FR-5 规则式锚定、新增出图自检 FR 与 backlog 指针）。
9. pytest 全绿 + 三图回归渲染（确认内置守卫零误报）→ 提交。