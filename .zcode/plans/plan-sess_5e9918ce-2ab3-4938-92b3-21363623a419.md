产物归档规范化：一图一项目文件夹（out/projects/<图名>/）

1. **CLI 能力**（src/quantchart/cli.py）：`chartflow run` 新增 `--project DIR`；config 顶层新增可选 `project` 字段（二者都有时 --project 优先）。给定了 project 时：`-o`/`--html` 未显式指定则默认派生 `<project>/chart.png`、`<project>/chart.html`，运行成功后自动把配置 YAML 快照复制为 `<project>/config.yaml` 并确保目录存在；不给 project 时行为完全不变（向后兼容）。
2. **config.py**：`project` 字段校验（字符串路径）。
3. **四份 YAML**：daily_candle.yaml / chart_01_xau.yaml / chart_02_cu0.yaml / chart_03_tl0.yaml 各加 `project: out/projects/<im2612_15min|chart_01_xau|chart_02_cu0|chart_03_tl0>`。
4. **存量迁移**（一次性，out/ 不入 git 故纯文件操作）：
   - out/projects/im2612_15min/ ← daily_candle_IM.png/html、_compare.png→compare.png、第一批次调试截图 _ref_*/_zoom_* → refs/
   - out/projects/chart_01_xau/ ← chart_01_xau.png/html、out/refs/chart_01/ → refs/、_compare_01.png → compare.png
   - chart_02_cu0 / chart_03_tl0 同理（含 fix1 证据）
   - 迁移后 out/ 根部不再有散文件；数据 CSV 按约定留 data/ 共享区
5. **README 五之二**：工作流图 ⑥ 节点与目录约定更新为项目文件夹布局（"一图一文件夹：config 快照/PNG/HTML/refs/compare"），四图归位后路径同步（verify CLI 示例路径改 out/projects/...）。
6. **测试**：新增 CLI --project 冒烟测试（tmp 目录跑 daily_csv 夹具 → 断言 project/config.yaml 与 chart.png 生成、-o 显式指定时优先）。
7. **验证**：pytest 全绿 → 四图按新布局重跑确认归位 → 提交。